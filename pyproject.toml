[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "inferno"
version = "0.1.0"
description = "A biodiversity and eDNA analysis toolkit"
requires-python = ">=3.12"

authors = [
    {name = "Onur Doğan"}
]
